getImportantGenes: scrapy for genetests site (not currently needed as GeneReviews and Ensembl provide their own annotations)

lightning: contains the prototype website and models used to run lightning
