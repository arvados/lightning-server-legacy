from django.test import TestCase

# Create your tests here.

#Search for Gene tests:
    #Search empty string -> alert box
    #Search for a non-existant gene -> alert box
    #Search for a gene with exactly one match -> addGeneAnnotation
    #Search for a gene with multiple matches -> choose one, then add Gene Annotation

#Load All Genes tests:
    #Load all genes -> Loads all genes in database

